node_modules
.next
.next-dev
dist
coverage
# Cualquier archivo de entorno queda fuera de git (ahí van contraseñas y claves).
# La única excepción es `.env.example`, que solo tiene valores de ejemplo.
.env*
!.env.example
*.log
uploads
